# Gemini API key (optional). Without it, generation falls back to Thai templates.
VITE_GEMINI_API_KEY=

# Gemini model override (optional). Defaults to gemini-3.1-flash-lite.
# Use a model available to your API key, e.g. gemini-2.0-flash or gemini-3.1-flash-lite.
VITE_GEMINI_MODEL=